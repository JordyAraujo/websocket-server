version: "3.9"

services:
  websocket-server:
    build: .
    container_name: websocket-server
    ports:
      - "3001:3001"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run dev
    environment:
      - NODE_ENV=development
    restart: unless-stopped
